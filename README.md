# JWT-RBAC-Templete
Django (DRF + SimpleJWT) + Next.js (App Router) + PostgreSQL full-stack starter. Secure JWT auth with HttpOnly cookies, token refresh/rotation/blacklist, and RBAC with custom roles &amp; defined permissions.
